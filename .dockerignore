.git
.DS_Store
bin
/agent
/eval
/localmodeleval
/realeval
/routeprobe
/scorefile
/yassai
/output
/input
*.log
.env
.env.*
eval-results
lib
# Large / irrelevant to the image build (only ./cmd/agent is compiled).
inspo
.github
docs
finetune
leaderboard-watch
models
scripts
testdata
tmp
assets/taskclf_backup_*
# Ship the fp32 classifier (the int8 quant is BROKEN: code_debugging
# over-fires ~0.6 on everything). The 4 GB judge VM handles fp32 because the
# agent classifies ONCE up front and frees the ONNX arena before any
# llama-server spawns - phases peak sequentially, never together.
assets/taskclf/model.int8.onnx
# Never ship seeded/host-generated agent memory into the build context.
MEMORY.md
memories/
