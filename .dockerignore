.git
.DS_Store
bin
/agent
/eval
/localmodeleval
/realeval
/routeprobe
/scorefile
/yassai
/output
/input
*.log
.env
.env.*
eval-results
lib
# Large / irrelevant to the image build (only ./cmd/agent is compiled).
inspo
.github
docs
finetune
leaderboard-watch
models
scripts
testdata
tmp
assets/taskclf_backup_*
# int8 quant is BROKEN (code_debugging over-fires ~0.6 on everything - a
# syllogism scores as code; caught by TestClassifyIntegration the first time
# CI actually loaded it). Ship the CI-validated fp32; image stays far under
# the 10GB cap.
assets/taskclf/model.int8.onnx
# Never ship seeded/host-generated agent memory into the build context.
MEMORY.md
memories/
